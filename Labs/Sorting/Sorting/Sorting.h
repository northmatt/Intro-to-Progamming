#pragma once

#include <string>
#include <iostream>

using std::string;
using std::nothrow;
using std::cout;

//smallest to largest length
void SortS(string inArray[], unsigned int size);
//least to largest
void SortI(int inArray[], unsigned int size);
//a - z
void SortC(const char *test[], unsigned int size);